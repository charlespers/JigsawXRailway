[phases.setup]
# Use the full Python distribution so we get pip and standard tooling
nixPkgs = ["python311Full"]

[phases.install]
# Use python -m pip to ensure we’re using the interpreter from nixPkgs
cmds = ["cd backend && python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend/api && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"

